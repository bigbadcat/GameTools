


--------------------已完成--------------------
数据定义文件拆分包含
导出完成后执行额外的Bat

分隔符简化成数组和成员两种，切有如下约束规则，类型可以添加IsSimple标记
  -(有IsSimple标记的类型才能作为成员类型，若作为数组成员类型，则自身不能再包含数组)
  -(有IsSimple标记的类型不能再包含其他任何类型)
实现并测试类型字段导出(成员类型有无数组字段两种情况)
测试类型数组导出

测试各种基础数据类型和枚举导出
实现并测试数组导出

开发三状态选中treeview
主界面布局
枚举定义页面布局
配置文档管理者
枚举定义与加载
枚举定义查看和生成代码
导出配置加载与显示
导出过程输出表现
数值导出与Unity加载
数据结构定义加载
导出修改
生成基础数据结构定义和加载代码
